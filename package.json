{
  "name": "react-vite-ssr",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cd packages/wilson && yarn build && cd ../.. && yarn dev",
    "prod": "cd packages/wilson && yarn build && cd ../.. && yarn build && yarn serve",
    "dev": "wilson",
    "build": "wilson build",
    "serve": "wilson serve"
  },
  "dependencies": {
    "preact": "^10.5.13",
    "preact-iso": "^1.1.1"
  },
  "devDependencies": {
    "@prefresh/vite": "^2.2.0",
    "@types/node": "^14.14.34",
    "preact-render-to-string": "^5.1.19",
    "prettier": "^2.2.1",
    "sass": "^1.32.8",
    "typescript": "^4.2.3"
  }
}
